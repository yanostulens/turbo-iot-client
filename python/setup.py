from setuptools import setup

setup(name='turbo_client',
      version='0.1',
      description='A client to manage a device connected to turbo-iot server',
      author='Nokia boyz',
      author_email='',
      license='MIT',
      packages=['turbo_client'])
